{
  "name": "openspace",
  "version": "1.0.0",
  "description": "Open Space planner",
  "author": "AIT GmbH",
  "license": "MIT",
  "scripts": {
    "backend": "cd Web && dotnet watch run",
    "frontend": "cd Web && npm run watch",
    "watch": "concurrently \"npm run backend\" \"npm run frontend\"",
    "postinstall": "cd Web && npm ci"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^3.5.1",
    "wait-on": "^2.1.0"
  }
}
